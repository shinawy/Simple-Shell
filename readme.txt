To build the program change the dir to existing makefile and use the command 'make'

To run the program directly without buliding the intermediate files type 'make run'

To delete the intermediate files created using make command use 'make clean'
